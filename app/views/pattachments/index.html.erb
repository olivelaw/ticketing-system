<h1>Listing pattachments</h1>

<table>
  <tr>
    <th>Project</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pattachments.each do |pattachment| %>
  <tr>
    <td><%= pattachment.project_id %></td>
    <td><%= link_to 'Show', pattachment %></td>
    <td><%= link_to 'Edit', edit_pattachment_path(pattachment) %></td>
    <td><%= link_to 'Destroy', pattachment, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pattachment', new_pattachment_path %>
