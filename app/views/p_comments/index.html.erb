<h1>Listing p_comments</h1>

<table>
  <tr>
    <th>Project</th>
    <th>User</th>
    <th>Comment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @p_comments.each do |p_comment| %>
  <tr>
    <td><%= p_comment.project_id %></td>
    <td><%= p_comment.user_id %></td>
    <td><%= p_comment.comment %></td>
    <td><%= link_to 'Show', p_comment %></td>
    <td><%= link_to 'Edit', edit_p_comment_path(p_comment) %></td>
    <td><%= link_to 'Destroy', p_comment, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New P comment', new_p_comment_path %>
